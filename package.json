{
  "name": "@radianos/resources",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "publish:packages": "pnpm --recursive --filter './packages/*' publish --access public"
  }
}
